# fraud-detection-cost-optimised-model
Cost-based credit card fraud detection. Evaluated Logistic Regression, XGBoost and Random Forest, then selected model using business-aligned threshold optimisation (FN = 100× FP) to minimise expected loss. Final chosen threshold: 0.025.
